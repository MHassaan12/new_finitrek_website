import { FunctionComponent } from "react"
import styles from 'businessAuth.module.css'


const BusinessAuth: FunctionComponent = ({ }) => {
    return (
        <section className={styles.parent}>
            <div>

            </div>
        </section>
    )
}

export default BusinessAuth;